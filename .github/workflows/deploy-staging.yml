name: Deploy Staging

on:
  push:
    branches: [main]

jobs:
  Setup:
    runs-on: ubuntu-latest
    steps:
      - name: Setup | Start
        run:  echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
